
struct KernelRegression<InputType>
{
   /// all the training inputs
   inputs: Vec<InputType>,
   // all the training outputs, one per input row
   //outputs,
   // matrix of distance between all inputs
   //distance_matrix
}